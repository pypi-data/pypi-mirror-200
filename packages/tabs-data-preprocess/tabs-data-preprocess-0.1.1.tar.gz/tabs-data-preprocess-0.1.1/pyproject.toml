[build-system]
requires = [
    "pdm-pep517>=1.0",
]
build-backend = "pdm.pep517.api"

[project]
name = "tabs-data-preprocess"
version = "0.1.1"
description = ""
authors = [
    { name = "Alexis Rosuel", email = "rosuelalexis1@gmail.com" },
]
dependencies = [
    "tabs-data-download>=0.1.4",
]
requires-python = ">=3.11"
readme = "README.md"

[project.license]
text = "MIT"

[project.scripts]
tabs-data-preprocess = "tabs_data_preprocess.main:run"

[tool.setuptools]
include-package-data = true

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = [
    "src",
]
exclude = [
    "tests",
]
namespaces = true

[tool.pdm.dev-dependencies]
dev = [
    "pytest>=7.2.2",
    "pytest-cov>=4.0.0",
]

[tool.pytest.ini_options]
pythonpath = [
    "src",
]
addopts = "--cov=tabs_data_preprocess"

[tool.black]
line-length = 88
target-version = [
    "py39",
]
include = "\\.pyi?$"
preview = true

[tool.flake8]
per-file-ignores = [
    "__init__.py:F401",
]
max-line-length = 88
count = true

[tool.isort]
known_third_party = []
known_first_party = [
    "tabs_data_preprocess",
]
