### requirements_google_storage.txt

- only supports pinning style of `package` or `package==package_version` (no >=, <=, ...etc)

### requirements.txt

- gs-pip-install package is a hard requirement for setup.py's CustomInstall
