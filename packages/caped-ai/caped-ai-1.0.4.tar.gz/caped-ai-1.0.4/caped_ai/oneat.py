
from oneat.NEATModels import NEATLRNet, NEATFocus, NEATFocusPredict, NEATPredict, NEATResNet, NEATTResNet, NEATVollNet



class NEATTResNet(NEATTResNet):
    def __init__(self, config, model_dir, model_name, catconfig, cordconfig):
        
        return super().__init__(self, config, model_dir, model_name,  catconfig=catconfig, cordconfig=cordconfig)
    
    
class NEATVollNet(NEATVollNet):
    def __init__(self, config, model_dir, model_name, catconfig, cordconfig):
        
        return super().__init__(self, config, model_dir, model_name,  catconfig=catconfig, cordconfig=cordconfig)
    
    
class NEATLRNet(NEATLRNet):
    
    def __init__(self, config, model_dir, model_name, catconfig, cordconfig):
        
        return super().__init__(self, config, model_dir, model_name,  catconfig=catconfig, cordconfig=cordconfig)
    
class NEATFocus(NEATFocus):
    def __init__(self, config, model_dir, model_name, catconfig, cordconfig):
        
        return super().__init__(self, config, model_dir, model_name,  catconfig=catconfig, cordconfig=cordconfig)
    
    
class NEATFocusPredict(NEATFocusPredict):
    
    def __init__(self, config, model_dir, model_name, catconfig, cordconfig):
        
        return super().__init__(self, config, model_dir, model_name,  catconfig=catconfig, cordconfig=cordconfig)
            
class NEATPredict(NEATPredict):
    def __init__(self, config, model_dir, model_name, catconfig, cordconfig):
        
        return super().__init__(self, config, model_dir, model_name,  catconfig=catconfig, cordconfig=cordconfig)
    
    
class NEATResNet(NEATResNet):
    
    def __init__(self, config, model_dir, model_name, catconfig, cordconfig):
        
        return super().__init__(self, config, model_dir, model_name,  catconfig=catconfig, cordconfig=cordconfig)
                       