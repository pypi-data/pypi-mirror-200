[project]
dynamic = ["version"]
name = "ml-indie-tools"
# version = "0.8.170"
license = { file = "LICENSE" }

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"
