apiVersion: v1
kind: PersistentVolume
metadata:
  name: airflow-dags-pv
  labels:
    app: airflow-dags-pv
spec:
  storageClassName: standard

  capacity:
    storage: 1Gi
  accessModes:
    - ReadOnlyMany
  # claimRef:
  #   apiVersion: v1
  #   kind: PersistentVolumeClaim
  #   name: default/airflow-pv
   # namespace: airflow
  hostPath:
    path: /home/docker/projects/gad-deliveries/__DEPLOYMENT_DIR__/airflow/dags