[tool.poetry]
name = "netbox-storage"
version = "0.0.0.0.943"
description = "Netbox Storage Plugin"
authors = [
    "Tim Rhomberg <timrhomberg@hotmail.com>",
]
readme = "README.md"
packages = [{include = "netbox_storage"}]

[tool.poetry.dependencies]
python = "^3.7"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"